{
  "$id": "https://doplexity-3d.dev/schema/persistence/room.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Room",
  "description": "One room, an axis-aligned box. Indoors it has walls with doorways cut into them. Outdoors set `open`: the perimeter still stops you, but nothing is drawn there, so a street ends in empty space rather than a wall. `blocks` are the solid masses standing inside it (buildings on a street), which you walk around, not through.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "What the room is for, in words: kitchen, living room, reception. Shown on the floor plan. Omitted means it goes by its id."
    },
    "position": {
      "$ref": "https://doplexity-3d.dev/schema/persistence/vec3.json"
    },
    "size": {
      "$ref": "https://doplexity-3d.dev/schema/persistence/vec3.json"
    },
    "floorKit": {
      "type": "string",
      "description": "asset-registry id (or kit family tag) for the floor pieces."
    },
    "wallKit": {
      "type": "string",
      "description": "asset-registry id (or kit family tag) for the wall pieces."
    },
    "objects": {
      "type": "array",
      "items": {
        "$ref": "https://doplexity-3d.dev/schema/persistence/object.json"
      }
    },
    "inventory": {
      "type": "array",
      "description": "Pick-up items available in this room.",
      "items": {
        "type": "object",
        "properties": {
          "itemId": {
            "type": "string"
          },
          "assetRef": {
            "type": "string"
          },
          "position": {
            "$ref": "https://doplexity-3d.dev/schema/persistence/vec3.json"
          }
        },
        "required": [
          "itemId"
        ],
        "additionalProperties": false
      }
    },
    "open": {
      "type": "boolean",
      "description": "True for open ground (a street, a plaza): the perimeter is a limit you cannot cross, drawn as nothing. Default false, which is a walled room."
    },
    "blocks": {
      "type": "array",
      "description": "Solid volumes standing inside this room: one per BUILDING on a street. Nothing walks through one; a door on a block's face leads to the instance inside it.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "position": {
            "$ref": "https://doplexity-3d.dev/schema/persistence/vec3.json"
          },
          "size": {
            "$ref": "https://doplexity-3d.dev/schema/persistence/vec3.json"
          },
          "assetRef": {
            "type": "string",
            "description": "Kit piece to face it with. Omitted means a plain mass."
          },
          "label": {
            "type": "string"
          },
          "floors": {
            "type": "integer",
            "minimum": 1,
            "description": "Storeys the mass holds, so its outside can be given a window per storey. Omitted is worked out from its height."
          },
          "program": {
            "type": "string",
            "description": "What the building is for, so its outside can be dressed to suit (a shop is glazed along the ground, a house is not)."
          }
        },
        "required": [
          "id",
          "position",
          "size"
        ],
        "additionalProperties": false
      }
    },
    "lights": {
      "type": "array",
      "description": "Where light comes from after dark: a lamp standing on the pavement, a sign over a door. Each says where it stands on the ground and what it is; how tall it is, what colour it burns and how many are lit at once are the renderer's business.",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "kind": {
            "type": "string",
            "enum": ["street_lamp", "sign", "ceiling"],
            "description": "street_lamp: a pole with a head on it. sign: the glow off a building's front, fixed to `blockId`. ceiling: what lights a room from above."
          },
          "position": { "$ref": "https://doplexity-3d.dev/schema/persistence/vec3.json" },
          "blockId": { "type": "string", "description": "sign: the mass it is fixed to." },
          "facing": { "type": "number", "description": "sign: which way it looks, in radians." }
        },
        "required": ["id", "kind", "position"],
        "additionalProperties": false
      }
    },
    "zones": {
      "type": "array",
      "description": "Flat surfaces marked out on the floor: the roadway, the pavement round a block, a square. They are walked over, never collided with; they say what the ground IS, so it can be surfaced differently and NPCs can be told where to walk.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": [
              "road",
              "sidewalk",
              "plaza"
            ],
            "description": "road: the carriageway. sidewalk: the pavement pedestrians keep to. plaza: open paving."
          },
          "position": {
            "$ref": "https://doplexity-3d.dev/schema/persistence/vec3.json"
          },
          "size": {
            "$ref": "https://doplexity-3d.dev/schema/persistence/vec2.json"
          },
          "assetRef": {
            "type": "string",
            "description": "Surface to face it with. Omitted means a plain one for its kind."
          }
        },
        "required": [
          "id",
          "kind",
          "position",
          "size"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "id",
    "position",
    "size",
    "floorKit",
    "wallKit"
  ],
  "additionalProperties": false
}
